apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AhedFinal.fullname" . }}-test-connection"
  labels:
    {{- include "AhedFinal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "AhedFinal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
